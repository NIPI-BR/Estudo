/**
 * Configuração do sender.
 *
 * CAST_APP_ID: o ID que o Google Cast Developer Console gera quando você
 * registra o receiver (https://cast.google.com/publish). Enquanto não tiver
 * o seu, dá para testar em modo não publicado — veja o README.
 */
const CAST_APP_ID = 'COLOQUE_SEU_APP_ID_AQUI';

/** Precisa ser exatamente o mesmo namespace usado no receiver. */
const CAST_NAMESPACE = 'urn:x-cast:com.nipi.signage';
